apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "variant-formatter.fullname" . }}-test-connection"
  labels:
{{ include "variant-formatter.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "variant-formatter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
