apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "molgenis-oauth2.fullname" . }}-test-connection"
  labels:
{{ include "molgenis-oauth2.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "molgenis-oauth2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
