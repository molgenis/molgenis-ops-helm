apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "molgenis-catalogue-etl.fullname" . }}-test-connection"
  labels:
    {{- include "molgenis-catalogue-etl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "molgenis-catalogue-etl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
