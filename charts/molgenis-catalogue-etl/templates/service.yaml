apiVersion: v1
kind: Service
metadata:
  name: {{ include "molgenis-catalogue-etl.fullname" . }}
  labels:
    {{- include "molgenis-catalogue-etl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "molgenis-catalogue-etl.selectorLabels" . | nindent 4 }}
