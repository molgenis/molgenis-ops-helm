apiVersion: v1
kind: Secret
metadata:
  name: kibana-credentials
  labels:
    app: molgenis-logging
    chart: Chart.yml
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
type: Opaque
data:
  auth: {{ .Values.kibana.authentication.htpasswordString }}