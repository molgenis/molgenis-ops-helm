questions:
  - variable: elasticsearch.authentication.username
    type: string
    required: true
    label: "Username Elastic user (for cluster)"
  - variable: elasticsearch.authentication.password
    type: password
    required: true
    label: "PAssword Elastic password (for cluster)"
  - variable: kibana.authentication.username
    type: string
    required: true
    label: "Username Kibana user (login)"
  - variable: kibana.authentication.username
    type: password
    required: true
    label: "Password Kibana user"
