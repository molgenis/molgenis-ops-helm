apiVersion: v1
kind: PersistentVolumeClaim
namespace: awx
metadata:
  name: public-static-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 15Gi

