questions:
  - variable: 'prometheus.alertmanagerFiles.alertmanager\.yml.global.slack_api_url'
    required: true
    default: "https://hooks.slack.com/services/..."
    label: "Slack API URL"
    description: "Can be found on https://molgenisdev.slack.com/services/BFLLSRULF"
    type: string
  - variable: "secret.molgenisMetrics"
    required: true
    label: "Prometheus User Password"
    description: "Password for the prometheus user on MOLGENIS deployments."
    type: string
  - variable: "secret.GITHUBTOKEN"
    required: true
    label: "Github token"
    description: "Github token for MOLGENIS deployments."
    type: string
  # - variable: "secret.cli2.json"
  #   required: true
  #   label: "Rancher login credentials"
  #   description: "Rancher cli login credentials for MOLGENIS deployments. Download the cli2.json and run in the commandline 'kubectl create secret generic test-secret --from-file=cli2.json -o yaml --dry-run' and copy the part after cli2.json: "
  #   type: string