apiVersion: v1
kind: Service
metadata:
  name: {{ include "cafe-molgenis.fullname" . }}
  labels:
    app: {{ include "cafe-molgenis.name" . }}
    chart: {{ include "cafe-molgenis.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: {{ include "cafe-molgenis.name" . }}
    release: {{ .Release.Name }}
