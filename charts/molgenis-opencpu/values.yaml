# Default values for opencpu.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

environment: production

service:
  name: opencpu
  type: ClusterIP
  port: 8004

opencpu:
  name: opencpu
  strategy:
    type: Recreate
  restartPolicy: Always
  image:
    repository: registry.hub.docker.com
    name: molgenis/opencpu
    tag: latest
    pullPolicy: Always
  resources:
    limits:
      cpu: 4
      memory: 3Gi
    requests:
      cpu: 200m
      memory: 1500Mi

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: 0
    nginx.ingress.kubernetes.io/whitelist-source-range: 195.169.23.1/24,195.169.22.1/24
  path: /
  hosts:
  - name: opencpu.molgenis.org
  tls: []

nodeSelector: {}

tolerations: []

affinity: {}
